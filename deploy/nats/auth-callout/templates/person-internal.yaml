# Personas con rol `admin` o `user`: consultas Y comandos. Ver README.md.
#
# Abre el prefijo de comandos ENTERO: qué método puede ejecutar cada rol lo recorta el mapa
# de core (`core/src/authorize-caller.ts`), que distingue `admin` de `user`. La plantilla no.

pub:
  allow:
    # El comodín va al final del prefijo de servicio, NUNCA en `{{instance}}.{{user_id}}.>`.
    - "{{instance}}.{{user_id}}.jiku-queries.v1.>"
    - "{{instance}}.{{user_id}}.jiku-commands.v1.>"

sub:
  allow:
    # El cliente DEBE fijar `inboxPrefix`, o las respuestas nunca llegan (síntoma: timeout).
    - "_INBOX.{{user_id_hash}}.>"
