---
id: REQ-006
title: "El contrato de consultas del bus — `jiku-queries` v1"
complexity: alta
status: formalized
created: 2026-08-24
ux_review: not-applicable
targets: [S-020, S-021, S-022, S-023, S-024, S-025, S-026, S-027, S-028]
---

# REQ-006: El contrato de consultas del bus — `jiku-queries` v1

## Requerimiento Original

> En @bus-api-consultas-ref.md tengo definida una nueva funcionalidad que implica desarrollar la
> api "jiku-queries" con todos los endpoints nats

**Fuente:** Mensaje del equipo
**Fecha de recepción:** 2026-08-24

**Documentos de análisis que son el insumo de este requerimiento** (sin trackear en la raíz del
monorepo, referenciados por S-013 como "no debe borrarse"):

- `bus-api-consultas-ref.md` — la referencia de implementación: los 23 endpoints, la gramática del
  payload, el contrato por recurso y las reglas.
- `bus-api-consultas.md` — el diseño completo y el porqué de cada decisión (§1 a §16).
- `bus-micro-dos-servicios.md` — la infraestructura del servicio, ya entregada por REQ-004.

## Contexto del Producto

**Feature groups relacionados:**

- **FG-3: Durabilidad y Observabilidad de la Escritura** — Es el grupo que trata el bus como
  superficie de operación. Este requerimiento **no cambia la durabilidad** (sigue request/reply sin
  JetStream) pero suma la mitad de lectura a la observabilidad: un segundo queue group con sus
  propios contadores y latencias en `nats micro stats`. Una consulta perdida, a diferencia de un
  comando perdido, es reintentable sin consecuencias: la regla dura que sí hereda es que **una
  request nunca se queda sin respuesta**.
- **FG-1: Onboarding de Usuarios y Permisos de Proyecto** — Dependencia indirecta y real: la clase
  del caller sale de `Usuario.roles`, que solo existe para quien ya tiene fila en `users`. Mientras
  el alta automática siga cubriendo solo a quien conecta al bus (REQ-005), un caller legítimo que
  nunca conectó recibe `unknown_caller`. Es la misma limitación que hoy produce el 401
  `user_not_found` en HTTP, trasladada a las consultas.
- **FG-6: Saneamiento del Modelo de Datos** — El contrato **no expone `ticketSlug`** ni lo acepta
  como filtro, porque la columna está marcada para eliminarse. Nacer sin ella evita que el
  saneamiento tenga que romper un contrato recién publicado.

**Stories relacionadas:**

- **S-011: Contrato del bus renombrado** — Fijó la gramática
  `{instance}.{user-id}.{svc}.{version}.{method}` sobre la que se montan los subjects de consulta.
- **S-012: `jiku-commands` servicio micro** — El servicio hermano en el mismo proceso. Este
  requerimiento **no lo toca**: la separación de queue group, pool y timeout ya está hecha.
- **S-013: `jiku-queries` registrado y descubrible** — **Es la story que este requerimiento
  completa.** Dejó 6 endpoints (`projects.list/get`, `tasks.list/get`, `comments.list/get`)
  **registrados y sin contrato**: aparecen en `nats micro info`, tienen queue group y contadores, y
  contestan un `failure` bien formado (`unknown_command`, "todavía no tiene contrato definido") vía
  `core/src/queries/pending.ts`, un archivo que el propio código declara transitorio y que
  desaparece con este REQ. También entregó la conexión de solo lectura del servicio.
- **S-014: Cliente de consultas de la api y separación 503/504** — Entregó `bus.query()` con su
  timeout propio de 10s, **deliberadamente sin ningún caller**. Este requerimiento pone a alguien a
  escuchar del otro lado, pero **no agrega el caller**: la api sigue leyendo Postgres directo.
- **REQ-005: Sincronización de usuarios y roles desde el bus** — **Dependencia bloqueante.** Define
  `Usuario.roles` e `identityType`, columnas que hoy no existen en el código y de las que depende la
  resolución de la clase del caller.
- **REQ-004: El bus en dos servicios micro** — Definió los dos servicios lógicos y dejó los 23
  endpoints de consulta enumerados en §9.2, apuntando a `bus-api-consultas.md` como fuente de verdad
  de la lista.
- **REQ-002: Eliminar el endpoint público de adjuntos** — Fijó que **todo acceso a un archivo exige
  sesión**. El contrato de consultas lo respeta: `files.get` devuelve metadatos y **no mintea
  ninguna URL**.

**Servicios mencionados:**

- **core**: implementa el servicio de consultas. Es el único servicio que cambia.
- **api**: **sin cambios.** Ni rutas GET migradas, ni el helper `sendQuery`, ni el mapa
  `httpStatusForQuery`. `bus.query()` sigue sin caller.
- **web / opus-web**: sin cambios. No hablan con el bus.
- **`@jiku/nats-protocol`**: gana los códigos de error nuevos del contrato de consultas
  (`unknown_caller`, `query_timeout`, `invalid_cursor`, `comment_not_found`, `task_not_found`).
- **`@jiku/models`**: **no se toca a propósito.** El servicio consulta con SQL explícito sobre su
  conexión de solo lectura, y las traducciones contrato ↔ base viven en el servicio de consultas.

**Flujos relacionados:**

- `docs/flows/autenticacion-y-entrada.md` — Donde se resuelve la identidad hoy. El servicio de
  consultas agrega un segundo lugar donde una identidad sin fila en `users` bloquea el acceso.
- `docs/flows/lectura-de-archivos.md` — El contrato declara explícitamente que la descarga y el
  preview **no son consultas**: siguen siendo el comando `files.{fileId}.request-download`.
- `docs/flows/adjuntos.md` — `attachments.list` y `files.get` exponen en lectura el modelo que
  REQ-001 separó en Archivo y Adjunto.

## Clarificaciones

### Alcance

- **P:** El documento §3.4 mapea 28 rutas `GET` de la api a consultas equivalentes, y §11 muestra
  cómo una ruta migrada consume `sendQuery`. ¿Hasta dónde llega este requerimiento?
- **R:** **Solo el servicio de consultas.** Los 23 endpoints responden por el bus con su contrato
  completo. La api sigue leyendo Postgres directo (ADR-001) y ninguna ruta `GET` migra. El helper
  `sendQuery` queda fuera.

- **P:** Hoy hay 6 endpoints registrados sin contrato. Al cerrar este requerimiento, ¿qué tiene que
  estar implementado?
- **R:** **Los 23 completos**, incluyendo `requirements.tags`, `settings` y `meta.describe`.
  `pending.ts` se elimina.

  > Nota de la aclaración: "sin contrato" describe el estado del **código**, no del documento.
  > S-013 registró esos endpoints como placeholders que aparecen en `nats micro info` y contestan un
  > `failure` bien formado, porque el contrato se definía después — justamente en
  > `bus-api-consultas.md`. El documento tiene todo; lo que falta es implementarlo.

- **P:** La clase del caller sale de `users.roles`, columna que hoy no existe: REQ-005 está en
  `captured`, sin implementar. ¿Cómo tratamos la dependencia?
- **R:** **REQ-005 es bloqueante.** Este requerimiento asume `Usuario.roles` e `identityType` ya en
  producción, y sus criterios de aceptación referencian la columna directamente.

### Funcionales

- **P:** La api consulta en modo conector, pero su clase también sale de la fila en `users`. La
  entrega del evento de autenticación **no es durable** (REQ-005: NATS sin JetStream, un evento
  perdido no se reintenta), así que si falta la fila del service user de la api, toda lectura vuelve
  403 `unknown_caller`. ¿Cómo se resuelve la clase de la api?
- **R:** **Lookup en `users`, igual que todos.** Sin excepciones ni lista de service users
  privilegiados en configuración: la api es un caller más y depende de tener su fila con rol
  `internal-app`. Si falta, sus consultas fallan hasta la próxima autenticación contra el bus.

- **P:** Un caller con rol `user` que publique directo al bus (no vía api) entra en modo interno: el
  servicio no le recorta nada a nivel de fila. Ve horas, ausencias, comentarios internos y todos los
  proyectos. ¿Es intencional en la v1?
- **R:** **Sí, intencional.** El modo interno no recorta filas. La autorización fina por rol
  (`admin` vs `user`) sigue siendo de la api sobre HTTP, y quien tiene credenciales de bus con rol
  `user` es equipo interno con acceso a todo.

### Negocio

- **P:** ¿Cuál es la prioridad?
- **R:** **Alta.** Es el contrato que habilita todo consumo de lectura por el bus y lo que desbloquea
  la migración posterior de las rutas `GET` de la api.

## Requerimientos Funcionales

### El servicio y su forma

1. `jiku-queries` sirve **23 endpoints** sobre 18 recursos, en request/reply sobre NATS, con el
   subject `{instance}.{user-id}.jiku-queries.v1.{recurso}.{operación}`. Los 6 endpoints hoy
   registrados sin contrato pasan a responder datos y el mecanismo `pendingContract` desaparece.
2. Hay **dos formas y solo dos**: `list` y `get`. `get` existe únicamente en los recursos con
   pantalla de detalle (`clients`, `projects`, `requirements`, `tasks`, `comments`, `files`); traer
   varios por id es `list` + `filter.id: [1,2,3]`. El id va **en el payload, nunca en el subject**.
3. El payload es un objeto JSON con **todo opcional** salvo `id` en un `get` (más `entityType` en
   `comments.get`), y cinco palancas: `filter`, `sort`, `page`, `fields`/`include` y `count`. En un
   `get`, `filter`, `sort`, `page` y `count` no aplican.
4. **Sin `*` en los subjects de consulta**: el id no forma parte del subject, para no inutilizar el
   cache de subjects del server con el tráfico de mayor volumen del sistema.

### La gramática de la consulta

5. `filter` es un objeto estructurado, no un string: **cada clave es un campo y todas se combinan
   con AND**. Igualdad por valor, `IN` por lista, `IS NULL` por `null`, negación con `{not}`, rangos
   con `gt`/`gte`/`lt`/`lte` combinables, y búsqueda de texto con `q` sobre los campos que declara
   cada recurso.
6. `filter.or` acepta una lista de alternativas que se combinan entre sí con OR y con el resto del
   filtro con AND, en **un solo nivel de anidamiento**.
7. Los filtros con forma propia se declaran por recurso: `tag` como par `{key, value}` exacto en
   `requirements` (acepta lista, con AND), y `entityType`/`entityId` en `comments`, `activity` y
   `subscriptions`, donde **`entityType` es obligatorio**.
8. **Se puede filtrar por un campo que no viene en la respuesta**: las listas de filtros y de campos
   son independientes (`users.email`, `requirements.finishedAt`, `people.mustChargeWorkedTime`).
9. `sort` es un array de campos aplicados en orden, con `-` adelante para descendente. Sin `sort` se
   usa el default del recurso, y el servicio **agrega `id` como último criterio de desempate
   siempre**, se pida o no.
10. Un campo se declara ordenable **solo si tiene índice compuesto terminado en `id`**: es lo que
    hace posible el keyset. `tasks.estimatedFinishDate` no es ordenable porque en la base es
    `VARCHAR`.

### Paginación

11. La paginación es **keyset con cursor opaco**: `limit` con default 50 y máximo 200 (un valor mayor
    se recorta sin avisar, `0` significa "usá el default"), y `cursor` ausente o `null` para la
    primera página.
12. El cursor está **atado a su `filter` + `sort`**: entre página y página los dos tienen que ir
    idénticos, `limit` sí puede cambiar. El cursor **no autoriza nada**: permisos y filtros se
    reaplican en cada página.
13. **La ausencia de `cursor` en la respuesta es la única señal de fin de colección.** `returned <
    limit` no significa nada.
14. El servicio arma la página **midiendo bytes**, no confiando en el `limit`: serializa item por
    item hasta ~50% del `max_payload` del server y corta devolviendo cursor. Un `body` que solo no
    entra en el presupuesto se trunca y se marca `bodyTruncated: true`.

### Qué campos vuelven

15. Cada recurso tiene un conjunto **base** que se devuelve cuando no se pide nada. Sobre eso:
    `conjunto devuelto = ( fields ?? base ) ∪ include ∪ { id }`.
16. `include` lleva **campos y relaciones en la misma lista**, y cada recurso declara cuál es cuál y
    la forma de cada relación. Las relaciones de colección vienen acotadas a los **10 más
    recientes** y marcan `"<relación>Truncated": true`.
17. Un campo nace **incluible y no base** si es calculado, texto sin cota o dato personal: meterlo en
    la base infla en silencio todas las consultas existentes. Por eso `description`,
    `totalMinutes`, `email` y `checksum` no vienen por defecto.
18. `count` tiene tres valores: `false` (default, solo items), `true` (items más `page.total`) y
    `"only"` (`page.total` con `items: []`, sin traer filas).

### Quién pregunta

19. **La identidad sale del segundo token del subject y solo de ahí** — es el `sub` de Zitadel de
    quien publica, infalsificable porque el auth-callout solo autoriza a publicar bajo el id propio.
    Un campo de identidad en el payload responde `invalid_fields`.
20. La clase del caller se resuelve **una vez, en el despachador**, buscando ese id en `users` y
    leyendo `Usuario.roles`: `internal-app` → **conector** (sin recorte, el caller autoriza por su
    cuenta), `user` → **interno** (sin recorte a nivel de fila), `external-user` → **externo**. Con
    varios roles **gana el más restrictivo** (`external-user` → `user` → `internal-app`).
21. Sin fila en `users`, con `roles` vacío o con roles desconocidos, la respuesta es
    **`unknown_caller`**, no una lista vacía. **La api no es excepción**: se resuelve por el mismo
    lookup y sin fila no consulta.
22. El recorte del **modo externo** se aplica **antes** del `filter`, no se puede desactivar por
    payload, y devuelve **cero resultados en vez de error**. Cada recurso declara su recorte;
    `worked-times`, `unworked-times`, `week-assigned-times` y `settings` **no tienen acceso externo**.
23. El **modo interno no recorta filas**: la autorización fina por rol sigue siendo de la api sobre
    HTTP. Es una decisión explícita de la v1.

### Fidelidad del contrato

24. **Un nombre no declarado en `filter`, `sort`, `fields` o `include` responde `invalid_fields`**,
    con `errorDetails` diciendo cuál y qué se aceptaba. **Nunca se ignora en silencio**: un filtro
    ignorado devuelve datos de más.
25. **Toda traducción contrato ↔ base vive en el servicio de consultas** y no se filtra a
    `@jiku/models`: `task` → `objectives`, `priority` enum ↔ entero (con `priorityValue` para no
    perder el 5), `body` → `new_value`, `authorId` → `changed_by`, `task_comment` →
    `objective_comment`, `properties` → `key_value_pairs`. La traducción de `entityType` va en las
    **dos direcciones**: en el filtro y en el valor devuelto.
26. Datos que **nunca** se exponen: `storageKey`, `storageBucket`, `storageRegion` y `ticketSlug`
    (ni como campo ni como filtro). Se excluyen siempre los vínculos con `deleted_at` y los archivos
    con `retentionStatus != active`.
27. **Esta API no mintea URLs.** Descarga y preview siguen siendo el comando
    `files.{fileId}.request-download`.
28. `settings` sirve una **lista blanca de claves** (`hours-per-day` y las cinco de archivos), no la
    tabla entera: una clave no declarada no existe para esta API.
29. `meta.describe` devuelve el contrato en datos —por recurso: `base`, `includable` (con
    `kind: field|relation` y el tope de las colecciones), `filterable`, `sortable`, `defaults` y los
    enums con etiquetas— y **se deriva de las mismas listas blancas que validan las consultas**.
    Reemplaza funcionalmente a `GET /unworked-times/reasons`, que no se elimina en este REQ.

### Errores y garantía de respuesta

30. El catálogo del contrato es `invalid_fields`, `invalid_cursor`, `unknown_caller`,
    `query_timeout`, `internal_error` y los `*_not_found` por recurso. `unknown_caller`,
    `query_timeout`, `invalid_cursor`, `comment_not_found` y `task_not_found` son **códigos nuevos**
    en `@jiku/nats-protocol`; `task_not_found` nace con el vocabulario de ADR-004 y
    `objective_not_found` se queda en los comandos.
31. **Los `*_not_found` no distinguen "no existe" de "no lo podés ver"**: responder 403 le
    confirmaría a un usuario externo que el recurso existe.
32. **Un `list` nunca devuelve `*_not_found`**: un filtro que no matchea nada es `items: []`. Los
    `*_not_found` solo salen de un `get`.
33. Los datos estructurados de un error van en `errorDetails`, nunca dentro de `errorMessage`. El
    mensaje al caller **no incluye el subject completo**, que lleva el user id.
34. **Toda request obtiene respuesta.** El `statement_timeout` de la conexión de lectura (8000 ms)
    queda estrictamente por debajo del timeout de la request (10000 ms), para que una consulta lenta
    vuelva `query_timeout` y no un timeout mudo del bus.
35. El servicio consulta con **su propia conexión de solo lectura, sin registrar los modelos de
    `@jiku/models`**, y con SQL explícito donde los nombres salen de las listas blancas y los valores
    van como parámetros. Registrar los modelos en una segunda instancia de Sequelize reasignaría las
    clases del ORM y rompería ADR-001 sin que nada lo diga.
36. `include` se resuelve con JOIN o con **una consulta por lote de la página**, nunca una consulta
    por item.

### Fuera de alcance

37. Queda fuera de este requerimiento, por decisión explícita:
    - **La api no cambia**: ninguna de las 28 rutas `GET` migra al bus, y el helper `sendQuery` con
      su mapa `httpStatusForQuery` no se implementa. `bus.query()` sigue sin caller.
    - **Agregaciones que colapsan filas**: los tres reportes de horas y
      `GET /projects/objectives-summary` se siguen sirviendo desde la api leyendo Postgres directo.
      La forma reservada para la v2 es `*.summary` con `groupBy` y `metrics` cerrados.
    - **Endpoint `batch`**: el bus ya multiplexa.
    - **Exportaciones masivas**: URL prefirmada, no por el bus.
    - **Recursos sin consumidor**: `project_status_updates`, orígenes, `resources`, hilos de mail.
    - **La durabilidad del bus**: sigue sin JetStream (FG-3).

## Criterios de Aceptación

### Registro y descubrimiento

1. DADO el proceso de `core` corriendo con los dos servicios micro
   CUANDO se ejecuta `nats micro info jiku-queries`
   ENTONCES aparecen los **23 endpoints** del contrato con su queue group propio, y ninguno responde
   `unknown_command` "todavía no tiene contrato definido" (el mecanismo `pendingContract` deja de
   existir)

### Gramática de la request

2. DADO un caller con rol `internal-app` y fila en `users`
   CUANDO publica `tasks.list` con
   `{"filter": {"projectId": 12, "state": ["backlog","activo"], "createdAt": {"gte":"2026-08-01T00:00:00.000Z"}}}`
   ENTONCES devuelve las tareas del proyecto 12 en esos dos estados creadas desde esa fecha — las
   tres claves combinadas con AND, la lista como `IN`

3. DADO el mismo caller
   CUANDO manda
   `"filter": {"responsiblePersonId": 77, "or": [{"state":"activo"}, {"state":"finalizado","finishedAt":{"gt":"2026-08-16T00:00:00.000Z"}}]}`
   ENTONCES aplica `responsiblePersonId = 77 AND (state = activo OR (state = finalizado AND finishedAt > …))`,
   con las alternativas de `or` en un solo nivel

4. DADO `requirements.list`
   CUANDO manda `"filter": {"q": "8140"}`
   ENTONCES busca por `id` porque el texto es solo dígitos; con texto no numérico busca en `title` y
   `description`

5. DADO un `list` sin `sort`
   CUANDO se ejecuta
   ENTONCES ordena por el default declarado del recurso (`tasks` → `["-createdAt"]`) y agrega `id`
   como último criterio de desempate, se pida o no

6. DADO `tasks.list` con `"sort": ["-priority","title"]`
   CUANDO se ejecuta
   ENTONCES ordena por la columna numérica `priority` descendente y después por `title` ascendente

### Campos: base, `fields` e `include`

7. DADO `tasks.get` con `{"id": 8140}` y nada más
   CUANDO se ejecuta
   ENTONCES devuelve exactamente el conjunto base del recurso — sin `description`, sin relaciones

8. DADO `requirements.get` con `{"id": 88, "include": ["description","project","totalMinutes"]}`
   CUANDO se ejecuta
   ENTONCES devuelve la base más esos tres, con `project` como `{id,name,code,status}` y
   `totalMinutes` = horas del requisito **más** las de sus tareas

9. DADO `tasks.list` con `{"fields": ["id","title","project"], "include": ["description"]}`
   CUANDO se ejecuta
   ENTONCES devuelve solo `id`, `title`, `project` y `description` — `fields` fija la lista,
   `include` suma encima, `id` siempre presente

10. DADO `requirements.get` con `include: ["comments"]` de un requisito con 25 comentarios
    CUANDO se ejecuta
    ENTONCES devuelve los 10 más recientes y marca `commentsTruncated: true`

### Paginación

11. DADO `tasks.list` con `"page": {"limit": 200}` sobre una colección de 500 filas
    CUANDO se recorre copiando `page.cursor` de cada respuesta con el mismo `filter` y `sort`
    ENTONCES cada página trae items sin repetir ni saltear, y la última **no trae `cursor`** — la
    ausencia de cursor es la única señal de fin (`returned < limit` no significa nada)

12. DADO un `list` cuyos items son grandes
    CUANDO la serialización de la página alcanza ~50% de `max_payload`
    ENTONCES corta ahí, devuelve menos items que el `limit` pedido y entrega cursor para seguir

13. DADO `tasks.list` con `"count": true`
    CUANDO se ejecuta
    ENTONCES devuelve los items y `page.total`; con `"count": "only"` devuelve `page.total` con
    `items: []` sin traer filas; sin `count` no viene `total`

### Identidad y clase del caller

14. DADO el subject `dev.323332022539911171.jiku-queries.v1.tasks.list`
    CUANDO llega la request
    ENTONCES la identidad se resuelve del **segundo token del subject** y de ningún otro lugar, y se
    busca esa fila en `users` para leer `roles` — la api no es excepción: también se resuelve por
    lookup

15. DADO un caller con `roles: ["admin","user"]`
    CUANDO consulta cualquier recurso
    ENTONCES entra en modo **interno** y el servicio no le recorta nada a nivel de fila; con
    `["user","external-user"]` gana el más restrictivo y entra en modo **externo**

16. DADO un caller con rol `external-user` y permiso sobre el proyecto 12
    CUANDO publica `requirements.list` sin filtro de proyecto
    ENTONCES devuelve solo requisitos del proyecto 12 con `visibilityLevel = public`, con el recorte
    aplicado **antes** del `filter` y sin forma de desactivarlo por payload

17. DADO un caller en modo externo
    CUANDO consulta `worked-times.list`, `unworked-times.list`, `week-assigned-times.list` o
    `settings.list`
    ENTONCES devuelve `items: []` — sin acceso, no un error

### Traducciones contrato ↔ base

18. DADO que en la base la tabla es `objectives` y `priority` es un entero 0-5
    CUANDO se consulta `tasks.list`
    ENTONCES el recurso se llama `tasks`, cada item trae `priority` como nombre de enum (5 se lee
    `urgente`) y `priorityValue` con el entero crudo, y filtrar acepta las dos formas

19. DADO `comments.list` con `{"filter": {"entityType": "task", "entityId": 8140}}`
    CUANDO se ejecuta
    ENTONCES lee `objective_activity` con `type_of_activity = 'comment'`, devuelve `body` (de
    `new_value`) y `authorId` (de `changed_by`), e incluye `attachments` excluyendo los vínculos con
    `deleted_at` y los archivos con `retentionStatus != active`

20. DADO `attachments.list` con `{"filter": {"entityType": "task_comment"}}`
    CUANDO se ejecuta
    ENTONCES traduce a `objective_comment` para consultar y devuelve `entityType: "task_comment"` en
    la respuesta — la traducción va en las dos direcciones y vive en el servicio de consultas, no en
    `@jiku/models`

### Recursos con forma propia

21. DADO `requirements.tags` con `{"filter": {"projectId": 12, "key": "modulo"}}`
    CUANDO se ejecuta
    ENTONCES devuelve `[{key: "modulo", values: [...]}]` con los valores que ya existen en los
    requisitos de ese proyecto

22. DADO `settings.list`
    CUANDO se ejecuta sin filtro
    ENTONCES devuelve solo las claves de la lista blanca (`hours-per-day` y las cinco de archivos) —
    una clave no declarada no existe para esta API

23. DADO `meta.describe` con `{"resources": ["tasks"]}`
    CUANDO se ejecuta
    ENTONCES devuelve `base`, `includable` (con `kind: field|relation` y el tope de las colecciones),
    `filterable`, `sortable`, `defaults` y los enums con etiquetas, **derivados de las mismas listas
    blancas que validan las consultas**

24. DADO `files.get` con `{"id": 4, "include": ["checksum"]}`
    CUANDO se ejecuta
    ENTONCES devuelve los metadatos del archivo y nunca `storageKey`, `storageBucket` ni
    `storageRegion`, y no mintea ninguna URL

### Separación lectura/escritura

25. DADO que el servicio usa su propia conexión de solo lectura sin registrar los modelos de
    `@jiku/models`
    CUANDO se ejecutan consultas y después un comando de escritura
    ENTONCES el comando sigue escribiendo con el usuario dueño (ADR-001 intacto) y las consultas
    siguen saliendo por el usuario de solo lectura

26. DADO `POSTGRESQL_STATEMENT_TIMEOUT_MS = 8000` y `NATS_QUERY_TIMEOUT_MS = 10000`
    CUANDO una consulta tarda más de 8 s
    ENTONCES la base corta primero y la respuesta es `query_timeout`, no un timeout mudo del bus

### Casos de error: nombres no declarados

27. DADO `tasks.list` con `{"filter": {"nombreInventado": 1}}`
    CUANDO se ejecuta
    ENTONCES responde `invalid_fields` con `errorDetails` indicando cuál fue el nombre rechazado y
    qué se aceptaba — **no se ignora en silencio ni se devuelven datos de más**

28. DADO que `tasks.estimatedFinishDate` es `VARCHAR` en la base y no está declarado ordenable
    CUANDO se manda `{"sort": ["estimatedFinishDate"]}`
    ENTONCES responde `invalid_fields`

29. DADO un `filter` con un `or` que contiene otro `or` adentro
    CUANDO se ejecuta
    ENTONCES responde `invalid_fields`: las alternativas admiten un solo nivel

30. DADO `tasks.list`
    CUANDO se manda `"page": {"limit": -5}` o un `limit` no entero
    ENTONCES responde `invalid_fields`; con `limit: 500` recorta a 200 sin avisar y con `limit: 0`
    usa el default de 50

31. DADO cualquier consulta
    CUANDO el payload incluye un campo de identidad (por ejemplo `userId` como quién pregunta)
    ENTONCES responde `invalid_fields`: la identidad sale del subject y solo de ahí

32. DADO `tasks.get`
    CUANDO se manda sin `id`, o con `filter`, `sort`, `page` o `count`
    ENTONCES responde `invalid_fields`: en un `get` esas cuatro palancas no aplican

33. DADO `comments.list`, `activity.list` o `subscriptions.list`
    CUANDO se ejecuta sin `filter.entityType`
    ENTONCES responde `invalid_fields`, y lo mismo `comments.get` sin `entityType`: son dos tablas y
    los ids se pisan

34. DADO `requirements.tags`
    CUANDO se ejecuta sin `filter.projectId`
    ENTONCES responde `invalid_fields`: es obligatorio

### Casos de error: cursor

35. DADO un `cursor` obtenido con `{"filter": {"projectId": 12}}`
    CUANDO se reusa cambiando el `filter` o el `sort`
    ENTONCES responde `invalid_cursor`; lo mismo con un cursor malformado o de otra versión. Cambiar
    solo el `limit` sí es válido

### Casos de error: caller desconocido

36. DADO un caller cuyo id del subject **no tiene fila en `users`**, o la tiene con `roles: []`, o
    solo con roles que no corresponden a ninguna clase conocida
    CUANDO consulta cualquier recurso
    ENTONCES responde `unknown_caller` — **nunca una lista vacía**, que se leería como "no hay datos"

37. DADO que el service user de la api perdió su fila en `users` (el evento de autenticación no es
    durable, REQ-005)
    CUANDO la api consulta
    ENTONCES recibe `unknown_caller` como cualquier otro caller, hasta que su próxima autenticación
    contra el bus reponga la fila — no hay excepción por configuración

### Casos de error: recurso no encontrado

38. DADO `tasks.get` con `{"id": 999999}`
    CUANDO ese id no existe
    ENTONCES responde `task_not_found`; en cambio `tasks.list` con un filtro que no matchea nada
    responde `items: []` — un `list` nunca devuelve `*_not_found`

39. DADO un caller en modo externo y un requisito de un proyecto sin permiso, o con
    `visibilityLevel: internal`
    CUANDO ejecuta `requirements.get` con ese id
    ENTONCES responde `requirement_not_found` y **no** un error de autorización: el contrato no
    distingue "no existe" de "no lo podés ver", porque distinguirlo confirmaría que el recurso existe

40. DADO `files.get` de un archivo con `retentionStatus` distinto de `active`
    CUANDO se ejecuta
    ENTONCES responde `file_not_found`

### Casos de error: fallas del servicio

41. DADO un error inesperado durante la ejecución de una consulta
    CUANDO se arma la respuesta
    ENTONCES responde un `failure` bien formado con `internal_error`, con los datos estructurados en
    `errorDetails` y sin incluir el subject completo en el mensaje al caller — el subject lleva el
    user id

42. DADO cualquier endpoint del contrato
    CUANDO llega una request, sea válida o inválida
    ENTONCES **siempre hay respuesta**: no existe el caso "el endpoint no contesta"

### Casos derivados de reglas del dominio

43. DADO que `HoraTrabajada.objectiveId` y `requirementId` son **mutuamente excluyentes** (regla que
    validan la api y `core` al escribir, sin constraint en la base)
    CUANDO se consulta `worked-times.list` con `filter.taskId` y `filter.requirementId` a la vez
    ENTONCES devuelve `items: []` porque las claves se combinan con AND y ninguna fila cumple las
    dos — no es un error de validación

44. DADO que `Persona` puede existir sin `Usuario` y que un `Usuario` con
    `identityType: "service"` no representa a nadie del equipo
    CUANDO se consulta `people.list` con `include: ["user"]`
    ENTONCES las personas sin usuario devuelven `user: null` sin fallar, y las identidades de
    servicio no aparecen como personas porque no tienen fila en `people`

45. DADO que un `Archivo` puede tener **0..N** `Adjunto` (un archivo sin vínculo es un estado válido)
    CUANDO un caller en modo externo ejecuta `files.get` de un archivo sin ningún vínculo
    ENTONCES lo ve solo si él mismo lo subió; si lo subió otro, responde `file_not_found`

## Clasificación

- **Complejidad:** alta
- **Justificación:**
  - **23 endpoints sobre 18 recursos**, cada uno con sus cuatro listas blancas (base, incluibles,
    filtrables, ordenables), su default de orden y su recorte del modo externo
  - Introduce **un motor de consulta genérico**, no endpoints ad-hoc: una gramática de `filter` con
    seis operadores y alternativas `or`, composición de `fields`/`include`, y paginación **keyset con
    cursor opaco atado a la consulta**. Es la pieza de mayor riesgo de diseño del requerimiento
  - La paginación tiene una restricción de infraestructura poco habitual: la página se corta
    **midiendo bytes** contra el `max_payload` del server, no por cantidad de items
  - **Tres clases de caller** con recorte distinto por recurso, resueltas desde datos persistidos, y
    un recorte que debe aplicarse antes del filtro y ser imposible de desactivar por payload
  - Todas las **traducciones contrato ↔ base** se concentran acá y no pueden filtrarse a
    `@jiku/models`, que es compartido con el único escritor
  - Toca la separación lectura/escritura: SQL explícito sobre una conexión de solo lectura **sin
    registrar los modelos del ORM**, con la trampa de que registrarlos funciona perfecto y rompe
    ADR-001 en silencio
  - Probablemente necesite **índices compuestos nuevos terminados en `id`** para cada campo que se
    declare ordenable: sin índice, el keyset no se sostiene. El alcance exacto es del diseño técnico
  - Suma **cinco códigos de error nuevos** al catálogo compartido, y uno de ellos (`task_not_found`)
    convive con su equivalente viejo (`objective_not_found`) que se queda en los comandos
  - Un solo servicio cambia (`core`) y ningún frontend, lo que acota el radio de la entrega
- **Prioridad:** alta
- **Dependencia bloqueante:** REQ-005 (`Usuario.roles` e `identityType` en producción)

## Impacto en Domain Entities

**Entidades nuevas:** ninguna. Es un contrato de lectura sobre el modelo existente.

**Modificaciones a entidades existentes:**

- **Usuario (`users`)**:
  - **Regla precisada (no nueva):** REQ-005 estableció que "la autorización de los callers del bus
    se resuelve con `Usuario.roles` persistido". Este requerimiento es **su primer consumidor
    real** y le da forma concreta: de `roles` sale una **clase de caller** con tres valores
    —conector, interno, externo— donde **con varios roles gana el más restrictivo**
    (`external-user` → `user` → `internal-app`), la clase se resuelve **una sola vez en el
    despachador** y viaja en el contexto de la consulta.
  - **Regla nueva:** una identidad **sin fila, con `roles` vacío o con roles desconocidos** no puede
    leer nada por el bus: la respuesta es `unknown_caller`, nunca una colección vacía. **Sin
    excepciones por identidad**, incluido el service user de la api. Como la fila la crea un evento
    de entrega no durable, la disponibilidad de la lectura por el bus **queda acoplada a la
    sincronización de `users`**.
  - **Consecuencia:** `roles` deja de ser un dato solo informativo del espejo de identidad y pasa a
    ser **el control de acceso efectivo** de toda la superficie de lectura del bus.

- **PermisoDeProyecto (`user_project_permissions`)**:
  - **Regla precisada:** la tabla que "sostiene todo el aislamiento del portal de clientes" gana un
    **segundo punto de aplicación**. Hasta ahora el aislamiento lo aplicaba la api con
    `validateProjectPermissions` antes de leer Postgres; desde este requerimiento el servicio de
    consultas la consulta por su cuenta para recortar a los callers en modo externo. Sigue **sin
    administrarse desde ninguna interfaz** del producto.

- **Requisito / Tarea / Actividad — el atributo `visibilityLevel`**:
  - **Regla precisada:** `visibilityLevel: 'public'` ya significaba "visible para usuarios externos
    autenticados" (REQ-002 le quitó todo efecto sobre el acceso anónimo). Este requerimiento agrega
    el **segundo lugar donde ese significado se aplica**: el recorte del modo externo del servicio
    de consultas exige `visibilityLevel = public` **además** del permiso de proyecto, y lo aplica
    antes del filtro del caller.

- **Tarea (`objectives`) — el atributo `priority`**:
  - **Regla precisada:** el escape transitorio que `core` usa para no perder el valor 5 (`priority`
    entero en la base, enum de nombres en el bus) se **formaliza en el contrato de lectura**: la
    consulta devuelve `priority` como nombre de enum **y** `priorityValue` con el entero crudo, y
    acepta filtrar por las dos formas. Deja de ser un escape y pasa a ser parte del contrato.

- **AjusteDelSistema (`system_settings`)**:
  - **Regla nueva:** la entidad deja de ser opaca para los consumidores. `settings.list` la expone en
    lectura, pero **solo por lista blanca de claves** (`hours-per-day` y las cinco de archivos): una
    clave no declarada **no existe** para esta API, aunque exista en la tabla. La escritura sigue
    siendo solo por SQL.

- **Archivo (`files`)**:
  - **Regla precisada:** los tres atributos de ubicación física (`storageKey`, `storageBucket`,
    `storageRegion`) pasan a ser **explícitamente no exponibles**, y el contrato de lectura declara
    que **no mintea URLs**: la obtención de bytes sigue siendo el comando `request-download`
    (REQ-001, REQ-002). `checksum` se expone solo bajo pedido y con la advertencia de que **nadie lo
    verifica**.

- **Proyecto (`projects`) — el atributo `ticketSlug`**:
  - **Decisión heredada, aplicada:** el contrato **no lo expone ni lo acepta como filtro** porque la
    columna está marcada para eliminarse (FG-6). Nace fuera del contrato para que el saneamiento no
    tenga que romperlo.

---

## Diseño Técnico

### Contexto Técnico

Documentos consultados:

**Arquitecturas**

- [x] `docs/architectures/core/manifest.yaml` — 12 convenciones, todas custom; módulos declarados
      hoy: `clients`, `projects`, `tasks`, `requirements`, `times`. **No declara `queries`**: la
      lista de módulos del manifiesto describe carpetas bajo `src/commands/`, y este REQ agrega
      una superficie hermana bajo `src/queries/` que el manifiesto todavía no nombra.
- [x] `docs/architectures/core/overview.md` — la decisión que define el servicio (único escritor,
      conexión con el usuario dueño), la transacción del despachador, "core no sabe de roles,
      permisos ni usuarios finales". Este REQ **acota esa última frase al plano de comandos**: el
      despachador de consultas sí resuelve una clase de caller desde `users.roles`.
- [x] `docs/architectures/core/conventions/contract-translation.md` — el lugar declarado de las
      traducciones contrato ↔ base. Gana cuatro traducciones nuevas de solo lectura.
- [x] `docs/architectures/core/conventions/error-handling.md` — el `Reply` de falla, el catálogo de
      códigos y su deuda; el despachador nunca lanza.
- [x] `docs/architectures/core/conventions/orm.md` — Sequelize con el usuario dueño. La conexión de
      lectura de `models/read.ts` es la excepción declarada: **no registra modelos**.
- [x] `docs/architectures/core/conventions/validation.md` — Joi por comando, un esquema por archivo.
- [x] `docs/architectures/core/conventions/testing.md` — Mocha + base real, entrando por el
      despachador.
- [x] `docs/architectures/api/manifest.yaml` — `type: backend`, no cambia. Solo recibe archivos de
      migración (ver "Cambios de Base de Datos").

**APIs**

- [x] `docs/apis/core.yaml` (AsyncAPI 2.6) — contrato de los 20 comandos. **Declara explícitamente
      que el contrato de `jiku-queries` está fuera de su alcance y que "cuando ese contrato exista
      recibe su propia spec"** (`docs/apis/core.yaml:27-32`). Es la instrucción que este diseño
      sigue: se crea `docs/apis/core-queries.yaml` y `core.yaml` queda como contrato de los
      comandos.
- [x] `docs/apis/core.yaml:968-993` — schema `Reply`: `status`, `errorCode`, `errorMessage`,
      `data`. **No tiene `errorDetails`**, que el contrato de consultas necesita desde el día uno.
- [x] `docs/apis/core.yaml:1113-1183` — schema `ErrorCode`: 26 valores. Faltan los cinco del
      contrato de consultas.
- [x] `docs/apis/api.yaml` — contrato HTTP. **No se toca**: ninguna de las 28 rutas `GET` migra.

**Schemas de BD**

- [x] `docs/db-schemas/jiku.md:592-926` — DBML de las 26 tablas. De acá salen los nombres reales de
      columna de las listas blancas: `objectives`, `objective_id`, `key_value_pairs`, `new_value`,
      `changed_by`, `storage_key`, `retention_status`, `type_of_activity`.
- [x] `docs/db-schemas/jiku.md:980-996` — "Inconsistencias del esquema": las siete condicionan el
      contrato. Tres lo condicionan directamente — `objectives.estimated_finish_date` es `VARCHAR`
      (por eso no es ordenable), `objectives.priority` es `INTEGER` 0-5 (de ahí `priorityValue`), y
      la exclusión `objective_id`/`requirement_id` de `worked_times` no tiene constraint (de ahí
      que un filtro con las dos claves devuelva `items: []` y no un error).
- [x] `docs/db-schemas/jiku.md:927-963` — "Estrategia de migraciones": `sequelize-cli`, archivos
      `.js` en `api/db-upgrade/migrations/`, con `POSTGRESQL_MIGRATION_USER`. **Es el único hogar de
      migraciones del producto** y es donde tienen que caer los índices de este REQ.
- [x] `docs/db-schemas/jiku.md:8-28` — "Quién escribe y quién lee": el rol de solo lectura ya tiene
      `SELECT` sobre todo el esquema; no hace falta ampliar permisos.

**ADRs relevantes**

- [x] `ADR-001` — separación lectura/escritura por credenciales. Este REQ la **ejerce por primera
      vez desde dentro del mismo proceso**: `readDb` con el rol de solo lectura contra `models/index`
      con el usuario dueño.
- [x] `ADR-003` — la transacción es del despachador. El despachador de consultas **no abre ninguna**
      y esa ausencia ya está codificada en `core/src/queries/types.ts`.
- [x] `ADR-004` — el vocabulario del producto vive en el contrato, no en el esquema. Es el ADR que
      autoriza `tasks` → `objectives`, `properties` → `key_value_pairs` y `priority` enum ↔ entero,
      y el que exige que la traducción **no** se filtre a `@jiku/models`.
- [x] `ADR-005` — modelos compartidos sin abrir conexión. Es la razón por la que `models/read.ts` no
      registra clases: dos instancias en el mismo proceso se pelean las clases del paquete.
- [x] `ADR-008` — deny-by-default. Aplica a la compuerta `ROLE_METHODS` y a las listas blancas del
      contrato: lo no declarado no se autoriza y lo no declarado no se consulta.
- [x] `ADR-002` / `ADR-007` — request/reply sin JetStream e identidad de Zitadel por auth-callout.
      Fijan que el segundo token del subject es infalsificable.

**Flujos del sistema**

- [x] `docs/flows/autenticacion-y-entrada.md` — dónde se resuelve la identidad hoy.
- [x] `docs/flows/sincronizacion-de-identidades.md` — cómo se puebla `users` (evento no durable).
- [x] `docs/flows/lectura-de-archivos.md` — la descarga es el comando `files.{fileId}.request-download`.
- [x] `docs/flows/vinculacion-de-archivos.md` — el modelo Archivo/Adjunto que `attachments.list` y
      `files.get` exponen en lectura.
- [x] `docs/flows/adjuntos.md` — **Deprecated** desde REQ-001; se cita por trazabilidad y no se
      modifica.
- [ ] **No existe un flujo de lectura por el bus.** Este REQ crea el primero.

**Código leído para fijar el punto de partida** (el contrato manda sobre el código, pero el diseño
tiene que saber qué existe):

- [x] `core/src/queries/{dispatcher,registry,types,index,pending}.ts` — la infraestructura de S-013:
      registry por `Map` con match exacto, contexto sin `transaction` y sin `params`, despachador que
      nunca lanza, y los 6 stubs `pendingContract`.
- [x] `core/src/models/read.ts` — `readDb` con `POSTGRESQL_READ_USER`, pool propio
      (`POSTGRESQL_READ_POOL_MAX`, 10) y `statement_timeout` de 8000 ms.
- [x] `core/src/authorize-caller.ts` — la compuerta de REQ-005, con `ROLE_METHODS` y la **exención
      del `CORE_TRUSTED_PUBLISHER_ID`**. Es el punto de mayor fricción del diseño (ver Riesgos).
- [x] `packages/nats-protocol/src/index.ts:234-320` — `Reply`, `failure()` y el `ErrorCode`.
- [x] `deploy/nats/auth-callout/templates/person.yaml` y `api.yaml` — los permisos de publicación
      usan `jiku-queries.v1.>`. **Los 17 endpoints nuevos no requieren tocar ninguna plantilla**:
      el `>` ya los cubre.
- [x] `deploy/.env:59-66,140-144` — la invariante `POSTGRESQL_STATEMENT_TIMEOUT_MS` (8000) <
      `NATS_QUERY_TIMEOUT_MS` (10000) ya está desplegada y documentada como invariante.

### Servicios Afectados

| Servicio | Impacto | Referencia | Cambios Necesarios |
|----------|---------|------------|-------------------|
| `core` | **Alto** | `docs/architectures/core/` | Todo el módulo `src/queries/`: motor de consulta genérico, 18 fichas de recurso con sus cuatro listas blancas, resolución de la clase del caller, recorte del modo externo, cursor keyset con presupuesto de bytes, `meta.describe` derivado. Se **elimina** `src/queries/pending.ts`. `QueryContext` gana `callerClass`. `Query` gana `validate()` |
| `@jiku/nats-protocol` | **Medio** | `packages/nats-protocol/src/index.ts:234-320` | `errorDetails` en el envelope `Reply` (campo nuevo, opcional), `failure()` con tercer parámetro opcional, y **5 códigos nuevos**: `unknown_caller`, `query_timeout`, `invalid_cursor`, `comment_not_found`, `task_not_found` |
| `api` | **Bajo — solo migraciones** | `docs/db-schemas/jiku.md:927-963` | **Ningún cambio de runtime**: ni rutas `GET` migradas, ni `sendQuery`, ni `httpStatusForQuery`. Recibe **archivos de migración** con ~17 índices compuestos, porque `api/db-upgrade/migrations/` es el único hogar de migraciones del producto |
| `@jiku/models` | **Nulo, y es deliberado** | `ADR-005`, `ADR-004` | **No se toca.** El servicio consulta con SQL explícito sobre `readDb`; las traducciones viven en el servicio de consultas |
| `web` / `opus-web` | **Nulo** | `ADR-006` | No hablan con el bus |
| `deploy` (NATS auth-callout) | **Nulo** | `deploy/nats/auth-callout/templates/` | `person.yaml` y `api.yaml` publican sobre `jiku-queries.v1.>`: el comodín ya cubre los 23 métodos |

> **Un solo servicio de runtime cambia.** Es lo que acota el radio de una entrega de 23 endpoints:
> no hay despliegue coordinado con los frontends, y el único consumidor posible —`bus.query()` de
> la api, entregado por S-014— **sigue sin caller** al terminar este REQ.

### Cambios de API

El contrato de este REQ es **el del bus**, no HTTP. `docs/apis/api.yaml` no cambia.

#### Documento nuevo: `docs/apis/core-queries.yaml`

`docs/apis/core.yaml:29-32` lo instruye textualmente:

> *"**The contract of `jiku-queries` is OUT OF SCOPE of this spec** — the endpoints are registered
> and discoverable, but their filters, pagination, fields and payloads are defined by a later
> requirement (RF-10 of REQ-004). Until then they answer a well-formed `failure`. **When that
> contract exists it gets its own spec**; this file stays the contract of the commands."*

- **Formato:** AsyncAPI 2.6, igual que `core.yaml` (`core` no expone HTTP; una spec OpenAPI tendría
  que inventar rutas — `docs/architectures/core/overview.md`).
- **Contenido:** los 23 canales, la gramática del payload (`filter`, `sort`, `page`, `fields`,
  `include`, `count`), el envelope de respuesta, el catálogo de errores del contrato, y las **cinco
  listas blancas por recurso** (base, incluibles, filtrables, ordenables, recorte externo).
- **Es la fuente de verdad**, con el mismo criterio que `core.yaml`: ante discrepancia con el
  código, manda el documento. Y es el documento del que **`meta.describe` tiene que ser el reflejo
  en datos** — derivado de las mismas listas blancas que validan, nunca mantenido a mano.

#### Endpoints existentes a modificar

**Los 6 registrados sin contrato** (ref: `core/src/queries/index.ts:16-24`, `pending.ts`) — hoy
responden `failure` con `unknown_command` y el mensaje *"todavía no tiene contrato definido"*:

| Endpoint | Estado actual | → Estado final |
|---|---|---|
| `projects.list` | `pendingContract` → `unknown_command` | → colección paginada de proyectos |
| `projects.get` | `pendingContract` → `unknown_command` | → un proyecto por id del payload |
| `tasks.list` | `pendingContract` → `unknown_command` | → colección paginada de tareas |
| `tasks.get` | `pendingContract` → `unknown_command` | → una tarea por id del payload |
| `comments.list` | `pendingContract` → `unknown_command` | → comentarios, con `entityType` obligatorio |
| `comments.get` | `pendingContract` → `unknown_command` | → un comentario, con `id` + `entityType` |

- **Cambio:** el mecanismo `pendingContract` **desaparece** y con él el archivo
  `core/src/queries/pending.ts`, que su propio comentario declara transitorio.
- **Justificación:** RF-1 y CA-1. `nats micro info jiku-queries` tiene que listar 23 endpoints y
  ninguno puede responder `unknown_command` "todavía no tiene contrato definido".

**`Reply` — el envelope compartido** (ref: `packages/nats-protocol/src/index.ts:234-239` y
`docs/apis/core.yaml:968-993`):

```ts
// ACTUAL
export interface Reply<T = unknown> {
  status: ReplyStatus;
  errorCode?: string;
  errorMessage?: string;
  data?: T;
}
```

```ts
// PROPUESTO
export interface Reply<T = unknown> {
  status: ReplyStatus;
  errorCode?: string;
  errorMessage?: string;
  errorDetails?: Record<string, unknown>;   // + NUEVO
  data?: T;
}
```

- **Cambio:** `+ NUEVO` campo opcional `errorDetails`, y `failure(errorCode, errorMessage, details?)`
  gana un tercer parámetro opcional (`packages/nats-protocol/src/index.ts:245-247`).
- **Justificación:** RF-33 y CA-27. Los datos estructurados de un error (`field`, `value`,
  `allowed`) van en `errorDetails` y **nunca dentro de `errorMessage`**. Sin el campo, el contrato
  repetiría la deuda que `error-handling` ya documenta: un llamador parseando texto con regex.
- **Compatibilidad:** es aditivo y opcional. Los 20 comandos siguen emitiendo `failure` de dos
  parámetros; la api parsea el envelope y **ignora campos que no conoce**.

**`ErrorCode` — el catálogo compartido** (ref: `packages/nats-protocol/src/index.ts:255-320` y el
`enum` de `docs/apis/core.yaml:1153-1183`):

| Código | Estado | Emisor | HTTP (cuando la api lo consuma) |
|---|---|---|---|
| `unknown_caller` | `+ NUEVO` | despachador de consultas | 403 |
| `query_timeout` | `+ NUEVO` | motor de consulta | 504 |
| `invalid_cursor` | `+ NUEVO` | motor de consulta | 400 |
| `comment_not_found` | `+ NUEVO` | `comments.get` | 404 |
| `task_not_found` | `+ NUEVO` | `tasks.get` | 404 |
| `invalid_fields` | reusado | validación del payload | 400 |
| `internal_error` | reusado | despachador | 500 |
| `client_not_found` · `project_not_found` · `requirement_not_found` · `file_not_found` | reusados | los `get` respectivos | 404 |
| `objective_not_found` | **intacto** | solo comandos | — |

- **Justificación:** RF-30. `task_not_found` nace con el vocabulario de `ADR-004` y **convive** con
  `objective_not_found`, que se queda en los comandos como la deuda que el propio ADR reconoce.
- **`unknown_caller` y no `caller_not_authorized`:** son dos preguntas distintas y las dos existen
  en el plano de consultas. `caller_not_authorized` (REQ-005) responde *"este caller no puede
  ejecutar este método"*; `unknown_caller` responde *"no se pudo determinar la clase del caller"*.
  Fusionarlas obligaría a `httpStatusForQuery` a mapear un mismo código a dos causas y, más grave,
  borraría el criterio de CA-36: sin fila la respuesta tiene que ser un error, **nunca una lista
  vacía**.
- **El mapa a HTTP queda fuera de alcance.** `api/lib/utils/bus/protocol.ts` **no se toca** y
  `httpStatusForQuery` **no se implementa** (RF-37): sin caller de `bus.query()`, ningún código de
  estos llega a HTTP en este REQ.

#### Nuevos endpoints necesarios

**17 endpoints nuevos**, que sumados a los 6 anteriores dan los **23** de RF-1. Todos sobre
`{instance}.{user-id}.jiku-queries.v1.{recurso}.{operación}`, **sin `*` en el subject** (RF-4: el id
viaja en el payload, para no inutilizar el cache de subjects del server con el tráfico de mayor
volumen del sistema).

| # | Método | Servicio | Descripción | Recorte externo |
|---|---|---|---|---|
| 1 | `clients.list` | core | Actores paginados | actores con al menos un proyecto permitido |
| 2 | `clients.get` | core | Un actor por id del payload | ídem |
| 3 | `requirements.list` | core | Requisitos paginados | proyectos permitidos **y** `visibilityLevel = public` |
| 4 | `requirements.get` | core | Un requisito | ídem |
| 5 | `activity.list` | core | Historial de cambios **y** comentarios; `filter.entityType` obligatorio | ídem |
| 6 | `people.list` | core | Personas del equipo | personas asignadas a proyectos permitidos |
| 7 | `users.list` | core | Usuarios (espejo de identidad) | usuarios con permiso sobre algún proyecto que el caller vea, más él mismo |
| 8 | `worked-times.list` | core | Horas trabajadas | **sin acceso** → `items: []` |
| 9 | `unworked-times.list` | core | Ausencias | **sin acceso** → `items: []` |
| 10 | `week-assigned-times.list` | core | Asignación semanal | **sin acceso** → `items: []` |
| 11 | `attachments.list` | core | Vínculo entidad ↔ archivo, con los datos del archivo aplanados | vínculos de entidades visibles |
| 12 | `files.get` | core | Metadatos de un archivo. **No mintea URLs** | archivos de entidades visibles; sin vínculo, solo quien lo subió |
| 13 | `subscriptions.list` | core | Suscripciones; `filter.entityType` obligatorio | solo las propias |
| 14 | `project-permissions.list` | core | Qué usuario ve qué proyecto | solo filas de proyectos permitidos |
| 15 | `requirements.tags` | core | Pares `{key, values[]}` existentes en los requisitos de un proyecto. `filter.projectId` **obligatorio** | tags de proyectos permitidos |
| 16 | `settings.list` | core | **Lista blanca** de claves de `system_settings` | **sin acceso** → `items: []` |
| 17 | `meta.describe` | core | El contrato en datos, **derivado de las mismas listas blancas que validan** | igual para todos |

**Forma del payload — `list`** (RF-3, RF-5 a RF-18):

```jsonc
{
  "filter":  { "projectId": 12, "state": ["backlog", "activo"],
               "createdAt": { "gte": "2026-08-01T00:00:00.000Z" },
               "or": [ { "state": "activo" },
                       { "state": "finalizado", "finishedAt": { "gt": "2026-08-16T00:00:00.000Z" } } ] },
  "sort":    ["-createdAt"],
  "page":    { "limit": 50, "cursor": null },
  "fields":  ["id", "title", "state"],
  "include": ["description", "project"],
  "count":   false
}
```

- **Todo opcional.** Las claves de `filter` se combinan **con AND**; el valor decide el operador:
  escalar → igualdad, lista → `IN`, `null` → `IS NULL`, `{not}` → distinto, `{gt|gte|lt|lte}` →
  rango combinable, `q` → búsqueda de texto sobre los campos que declara el recurso.
- **`filter.or`** admite **un solo nivel**: un `or` dentro de otro `or` es `invalid_fields` (CA-29).
- **`sort`** es un array aplicado en orden, `-` para descendente. El servicio **agrega `id` como
  último criterio de desempate siempre**, se pida o no (RF-9).
- **`page.limit`**: default 50, máximo 200. `> 200` se recorta **en silencio** y `page.limit`
  devuelve el efectivo; `0` significa "usá el default"; negativo o no entero es `invalid_fields`.
- **Conjunto devuelto:** `( fields ?? base ) ∪ include ∪ { id }` (RF-15).
- **`count`**: `false` (default) · `true` (items + `page.total`) · `"only"` (`page.total` con
  `items: []`, **sin ejecutar la consulta de filas**).

**Forma del payload — `get`** (RF-2):

```jsonc
{ "id": 88, "fields": [], "include": [] }        // + "entityType" en comments.get
```

`filter`, `sort`, `page` y `count` **no aplican**: si vienen, `invalid_fields` (CA-32).

**Envelope de respuesta:**

```jsonc
// list
{ "status": "success",
  "data": { "items": [ … ],
            "page": { "limit": 50, "returned": 50, "cursor": "eyJ…", "total": 128 } } }

// get   — `data` ES el recurso, sin envoltorio de colección
{ "status": "success", "data": { "id": 88, "title": "…" } }

// error
{ "status": "failure", "errorCode": "invalid_cursor",
  "errorMessage": "…", "errorDetails": { "field": "sort", "value": "-relevance", "allowed": [ … ] } }
```

- `items` **siempre presente**; `[]` no es un error.
- `page.limit` es el **efectivamente aplicado**, que puede ser menor al pedido.
- `page.returned` es explícito para que el recorte por bytes sea visible.
- **La ausencia de `page.cursor` es la única señal de fin de colección** (RF-13). `returned < limit`
  no significa nada: puede ser un recorte por presupuesto de bytes.
- `page.total` solo si se pidió `count`. Es exacto, no estimado.

### Cambios de Base de Datos

**Ninguna tabla nueva. Ninguna columna nueva. Ningún cambio de tipo.** Es un contrato de lectura
sobre el modelo existente: el esquema de `docs/db-schemas/jiku.md:592-926` alcanza tal cual está.

Lo único que cambia son **índices**, y no son opcionales: RF-10 declara un campo ordenable **solo
si tiene índice compuesto terminado en `id`**, porque es lo que hace posible el keyset. Sin índice,
la paginación degrada a un `Seq Scan` + `Sort` por página y el `statement_timeout` de 8000 ms
empieza a devolver `query_timeout` bajo carga normal.

#### Tablas existentes a modificar — solo índices

| Tabla | Índice a crear | Qué sostiene |
|---|---|---|
| `objectives` | `(project_id, created_at DESC, id DESC)` | `tasks.list` filtrado por proyecto con el sort default `["-createdAt"]` |
| `objectives` | `(priority DESC, created_at DESC, id DESC)` | `sort: ["-priority", …]` (CA-6) |
| `objectives` | `(state, created_at DESC, id DESC)` | `filter.state` + sort default |
| `requirements` | `(project_id, created_at DESC, id DESC)` | `requirements.list` por proyecto |
| `requirements` | `(state, created_at DESC, id DESC)` | `filter.state` + sort default |
| `requirements` | **GIN** sobre `tags` | el filtro `tag` usa el contains de `jsonb` (RF-7) y `requirements.tags` |
| `objective_activity` | `(objective_id, type_of_activity, created_at, id)` | `comments.list` (`type_of_activity = 'comment'`) y `activity.list` |
| `requirement_activity` | `(requirement_id, type_of_activity, created_at, id)` | ídem del otro lado |
| `people_objectives` | `(person_id, objective_id)` | `filter.responsiblePersonId` de `tasks` |
| `people_requirements` | `(person_id, requirement_id)` | `filter.responsiblePersonId` de `requirements` |
| `projects` | `(client_id, name, id)` | `projects.list` + `filter.clientId` con sort por `name` |
| `worked_times` | `(person_id, date DESC, id DESC)` | `worked-times.list` con sort default `["-date"]` |
| `worked_times` | `(project_id, date DESC, id DESC)` | ídem por proyecto |
| `worked_times` | `(requirement_id)` y `(objective_id)` | el campo calculado `requirements.totalMinutes` — **dos subconsultas correlacionadas por fila**; con `limit: 200` son 400 |
| `unworked_times` | `(person_id, date, id)` | `unworked-times.list` |
| `week_assigned_times` | `(person_id, date_from, id)` | `week-assigned-times.list` |
| `user_project_permissions` | `(user_id)` | **participa de TODA consulta en modo externo** |
| `attachments` | `(entity_type, entity_id)` | **ya existe** (`docs/db-schemas/jiku.md:813-816`) — no se crea |
| `projects` | `(client_id)` | **ya existe** (`20260724_01_add_index_projects_client_id`) — el compuesto de arriba lo sustituye funcionalmente y **no se borra** |
| `files` | `(uploaded_by, byte_status)` | **ya existe** — cubre el caso "archivo sin vínculo, solo quien lo subió" de `files.get` |

- **Estrategia de migración:** una migración `api/db-upgrade/migrations/YYYYMMDD_NN_query_indexes.js`
  con `CREATE INDEX` (aditiva, sin downtime). Rollback: `DROP INDEX` de los mismos nombres.
- **`CONCURRENTLY` es la decisión de la story**, no de este diseño: `sequelize-cli` corre cada
  migración dentro de una transacción por defecto y `CREATE INDEX CONCURRENTLY` no puede vivir
  adentro de una. Contra `objectives`, `requirements` y `objective_activity` en producción, el
  `CREATE INDEX` común toma un `SHARE` lock que **bloquea las escrituras** mientras construye.
- **`user_project_permissions(user_id)`: verificar antes de crear.** `docs/db-schemas/jiku.md:771-779`
  no declara índices sobre la tabla, pero es de decenas de filas y puede tener uno implícito por una
  constraint. Es una verificación de la story, no una decisión.

#### Nuevas tablas

Ninguna.

#### Permisos de base

El rol de solo lectura (`POSTGRESQL_READ_USER` = `DATABASE_READONLY_USER`, `deploy/docker-compose.yml:168-169`)
**ya tiene `SELECT` sobre todo el esquema**: es el mismo rol con el que la api lee hoy
(`docs/db-schemas/jiku.md:8-16`). **No hay `GRANT` nuevo que otorgar**, y esa es justamente la
garantía que ADR-001 pone en juego: una escritura desde el módulo de consultas falla por permisos de
PostgreSQL, no por disciplina de código.

### Flujo de Interacción

#### Escenario 1: `tasks.list` paginado, caller en modo conector (CA-2, CA-5, CA-11, CA-12)

**Trigger:** un caller con rol `internal-app` (hoy, el service user de la api) publica una consulta.

**Flujo:**

1. **[Caller]**
   - Publica en `dev.323332022539911171.jiku-queries.v1.tasks.list`
   - Payload: `{"filter": {"projectId": 12, "state": ["backlog","activo"]}, "page": {"limit": 200}}`
   - Espera respuesta en su inbox hasheado, con `NATS_QUERY_TIMEOUT_MS` = **10000 ms**
   - → llega al endpoint micro `tasks-list` del servicio `jiku-queries`, queue group `jiku-queries`

2. **[core · `bus/service.ts` → `handle()`]**
   - Decodifica el cuerpo (`msg.json()`; cuerpo vacío = `{}`)
   - → delega en `QueryDispatcher.dispatch(subject, payload)`

3. **[core · `queries/dispatcher.ts` — LA IDENTIDAD Y LA CLASE]**
   - `caller = callerFromSubject(subject)` → `323332022539911171`. **Del segundo token del subject
     y de ningún otro lugar** (RF-19, CA-14)
   - Compuerta de REQ-005: `authorizeCaller(caller, 'tasks.list', 'queries')`
   - **NUEVO — resolución de la clase**, con el mismo `SELECT roles FROM users WHERE id = :caller`:
     `roles: ['internal-app']` → clase **conector**. Se resuelve **una sola vez acá** y viaja en el
     contexto (RF-20)
   - → `query.validate(payload)` y después `query.execute(payload, { caller, callerClass, db })`

4. **[core · `queries/tasks/tasks-list.ts` — VALIDACIÓN CONTRA LAS LISTAS BLANCAS]**
   - `projectId` y `state` ∈ filtrables de `tasks`; sin `sort` → default `["-createdAt"]`;
     `limit: 200` ≤ máximo
   - Se agrega `id` como último criterio de desempate: `ORDER BY created_at DESC, id DESC`
   - Conjunto devuelto = base de `tasks` (sin `description`, que es incluible)
   - → arma el SQL

5. **[core · motor de consulta → `readDb`]**
   - SQL explícito sobre `objectives` (**la traducción `tasks` → `objectives` vive acá**, ADR-004),
     con los nombres desde las listas blancas y los valores como parámetros:
     `WHERE project_id = $1 AND state = ANY($2) ORDER BY created_at DESC, id DESC LIMIT 201`
   - `LIMIT limit + 1` para saber si hay página siguiente sin un `COUNT`
   - Conexión: `readDb`, rol de solo lectura, `statement_timeout` 8000 ms
   - → filas crudas

6. **[core · proyección y presupuesto de bytes]**
   - Traduce por fila: `priority` entero → nombre de enum **más** `priorityValue` con el entero crudo
     (RF-25, CA-18)
   - Serializa item por item midiendo bytes hasta `floor(nc.info.max_payload * 0.5)`; si se pasa,
     **corta ahí** y devuelve cursor aunque falten items del `limit` (RF-14, CA-12)
   - Cursor = base64url de `{"v":1,"k":[<created_at>,<id>],"h":<hash(filter+sort)>}`
   - Response: `200 success` con `{ items, page: { limit: 200, returned: 173, cursor: "eyJ…" } }`
   - → vuelve por el inbox del caller

7. **[Caller · página siguiente]**
   - Repite con `filter` y `sort` **idénticos** y `page.cursor` copiado tal cual
   - El servicio verifica `h` contra el `filter`+`sort` de esta request, **reaplica identidad,
     clase y permisos** (el cursor no autoriza nada) y resuelve con
     `WHERE (created_at, id) < ($k0, $k1)` sobre el índice
   - Repite hasta que **la respuesta no traiga `cursor`** (RF-13)

**Outcome:** el caller recorre la colección completa sin repetir ni saltear filas, con escrituras
concurrentes en curso, y sabe que terminó **solo** por la ausencia de cursor.

#### Escenario 2: `requirements.list` de un caller en modo externo (CA-16, CA-39)

**Trigger:** una persona con rol `external-user` conectada al bus con su propio token
(`deploy/nats/auth-callout/templates/person.yaml`) publica `requirements.list` **sin filtro de
proyecto**.

**Flujo:**

1. **[Persona]** publica `dev.<su-sub>.jiku-queries.v1.requirements.list` con `{}`
2. **[dispatcher]** `roles: ['external-user']` → clase **externo**. Con `['user','external-user']`
   **gana el más restrictivo** y también sería externo (RF-20, CA-15)
3. **[recorte del modo externo — ANTES DEL FILTER]** el servicio agrega, **sobre el SQL y no sobre
   el payload**:
   - `project_id IN (SELECT project_id FROM user_project_permissions WHERE user_id = $caller)`
   - `AND visibility_level = 'public'`
   - **No se puede desactivar por payload** (RF-22): un `filter.visibilityLevel = "internal"` se
     combina con AND contra el recorte y da **cero resultados**, no un error
4. **[motor]** aplica encima el `filter` del caller, el sort default y la paginación
5. **Response:** `success` con solo los requisitos públicos de los proyectos permitidos

**Outcome:** el aislamiento del portal de clientes gana un **segundo punto de aplicación**, además
de `validateProjectPermissions` en la api. Un `requirements.get` de un id no visible responde
`requirement_not_found` y **no** un error de autorización (CA-39): distinguirlos confirmaría que el
recurso existe.

#### Escenario 3: el caller sin fila en `users` — incluida la api (CA-36, CA-37)

**Trigger:** cualquier caller cuyo id del subject no tenga fila en `users`, o la tenga con
`roles: []`, o solo con roles fuera del mapa.

**Flujo:**

1. **[dispatcher]** `SELECT roles FROM users WHERE id = :caller` → sin fila / `[]` / desconocidos
2. **[dispatcher]** responde `failure` con `unknown_caller` — **nunca `items: []`**, que se leería
   como "no hay datos" y se debuggea durante horas
3. **La api no es excepción** (RF-21, CA-37): si su evento de autenticación se perdió (NATS sin
   JetStream, REQ-005), sus consultas fallan hasta que su próxima autenticación contra el bus
   reponga la fila. **No hay excepción por configuración.**

**Outcome:** la disponibilidad de la lectura por el bus queda **acoplada a la sincronización de
`users`**, y eso es una consecuencia aceptada y declarada, no un efecto colateral.

#### Manejo de errores — el catálogo completo

| Situación | Código | Dónde se decide |
|---|---|---|
| Nombre no declarado en `filter`/`sort`/`fields`/`include` | `invalid_fields` + `errorDetails: {field, value, allowed}` | validación del recurso (CA-27, CA-28) |
| `or` anidado dentro de otro `or` | `invalid_fields` | parser del filtro (CA-29) |
| `limit` negativo o no entero | `invalid_fields` | validación de `page` (CA-30) |
| Campo de identidad en el payload | `invalid_fields` | validación transversal (RF-19, CA-31) |
| `get` sin `id`, o con `filter`/`sort`/`page`/`count` | `invalid_fields` | validación de la forma `get` (CA-32) |
| `comments`/`activity`/`subscriptions` sin `filter.entityType` | `invalid_fields` | ficha del recurso (CA-33) |
| `requirements.tags` sin `filter.projectId` | `invalid_fields` | ficha del recurso (CA-34) |
| Cursor malformado, de otra `v`, o con `h` que no coincide | `invalid_cursor` | decodificador del cursor (CA-35) |
| Sin fila en `users` / `roles` vacío / roles desconocidos | `unknown_caller` | despachador (CA-36, CA-37) |
| `get` de un id inexistente **o no visible** | `{recurso}_not_found` | el `get` (CA-38, CA-39, CA-40) |
| `list` que no matchea nada | **`items: []`**, no un error | motor (CA-38) |
| `statement_timeout` de 8000 ms | `query_timeout` | captura del error de PostgreSQL (CA-26) |
| Cualquier otra cosa | `internal_error`, con detalle en `errorDetails` y **sin el subject completo** en el mensaje (lleva el user id) | `catch` del despachador (CA-41) |

**La garantía dura:** toda request obtiene respuesta (CA-42). Está sostenida por tres redes ya
existentes que este REQ **no debilita**: el `try/catch` de `QueryDispatcher.dispatch`
(`core/src/queries/dispatcher.ts:60-66`), el `try/catch` de `handle()` en `bus/service.ts`, y la
invariante `POSTGRESQL_STATEMENT_TIMEOUT_MS` (8000) < `NATS_QUERY_TIMEOUT_MS` (10000), que hace que
una consulta lenta vuelva `query_timeout` y no un timeout mudo del bus.

### Consideraciones Técnicas

#### Riesgos

- **Riesgo (el mayor del diseño): la exención de la api en `authorizeCaller` choca con CA-14 y
  CA-37.** `core/src/authorize-caller.ts` cortocircuita al `CORE_TRUSTED_PUBLISHER_ID` **sin
  consultar la base** —y con razón: sin esa rama, un evento de autenticación perdido produce una
  caída total y silenciosa de escritura—. Pero RF-21 exige que la clase del caller de las consultas
  salga de un lookup en `users` **para todos, la api incluida**.
  - **Mitigación (la decisión de este diseño): son dos preguntas distintas y se resuelven por
    separado.** `authorizeCaller` sigue respondiendo *"¿puede ejecutar este método?"* con su
    exención intacta; la resolución de la clase responde *"¿qué le recorto?"* y **no tiene
    exención**. Un caller exento de la compuerta que no tenga fila recibe `unknown_caller` de la
    resolución de clase — que es exactamente lo que CA-37 describe.
  - **No fusionar las dos en una sola compuerta.** Quitarle la exención a `authorizeCaller` para
    "unificar" reintroduce la caída total de escritura que su comentario documenta; fusionar los dos
    códigos borra el criterio de CA-36.

- **Riesgo: dos `SELECT` a `users` por consulta.** La compuerta ya hace `User.findByPk(caller)`; la
  resolución de clase necesita los mismos `roles`.
  - **Mitigación:** un solo lookup por request. Extraer la lectura a una función compartida que
    devuelva los `roles`, y que el despachador de consultas la invoque **una vez** y alimente con el
    resultado tanto a `rolesAuthorize()` como a la resolución de clase. Es un refactor acotado de
    `authorize-caller.ts`, sin cambio de comportamiento en el plano de comandos.
  - **Sin cache**, por el mismo criterio que ya fijó S-017 (CA-17 de REQ-005): cachear
    reintroduciría roles obsoletos con una ventana adicional y no medible, para ahorrar un `SELECT`
    por PK contra una tabla de decenas de filas.

- **Riesgo: `internal-app` tiene `queries: []` en `ROLE_METHODS`** (`core/src/authorize-caller.ts`),
  así que un caller con ese rol que **no** sea el publicador confiable es rechazado por la compuerta
  con `caller_not_authorized` y nunca llega a la clase **conector**.
  - **Mitigación / decisión:** `ROLE_METHODS` **no se toca en este REQ**. Es deny-by-default
    (ADR-008) y hoy el único `internal-app` es la api, que entra por la exención. Habilitar un
    segundo conector es un cambio deliberado en **dos** archivos —ese mapa y la plantilla del
    callout— y merece su propio requerimiento. Queda anotado porque el síntoma ("le di el rol
    `internal-app` y no puede consultar") es difícil de diagnosticar sin esta línea.

- **Riesgo: registrar los modelos de `@jiku/models` en `readDb` funciona perfecto y rompe ADR-001 en
  silencio.** Dos instancias de Sequelize en el mismo proceso se pelean las clases del paquete: la
  segunda que registre el índice las **reasigna**, y las escrituras empiezan a salir por la conexión
  equivocada sin un solo síntoma.
  - **Mitigación:** ya está codificada en `core/src/models/read.ts` (no recibe `models`) y verificada
    por `core/tests/models/read.test.ts` y por CA-6 de S-013 (un alta sigue funcionando **después**
    de que las consultas abrieron su conexión). **Este REQ no puede aflojarla**: 18 fichas de recurso
    son 18 tentaciones de "usar el ORM que ya está ahí". CA-25 lo mantiene como criterio explícito.

- **Riesgo: inyección de SQL.** El motor arma SQL dinámico a partir del payload del caller.
  - **Mitigación estructural:** los **nombres** de tabla, columna y dirección de orden salen
    **exclusivamente de las listas blancas** del recurso —nunca del payload— y los **valores** van
    siempre como parámetros (`replacements`). Un nombre no declarado no llega al SQL: responde
    `invalid_fields` antes. Es la misma regla que hace que `invalid_fields` sea obligatorio y no
    cosmético (RF-24): ignorar un filtro en silencio devuelve **datos de más**.

- **Riesgo: `meta.describe` mantenido a mano se desactualiza y pasa a mentir.** Un `sortable`
  declarado que después responde `invalid_fields` es un bug del servicio.
  - **Mitigación:** RF-29 y CA-23 lo exigen **derivado de las mismas estructuras que validan**. El
    diseño lo aprovecha al revés: la ficha de recurso es **un dato**, no código imperativo, y tanto
    el validador como `meta.describe` la leen. Si la ficha fuera código, esta garantía no sería
    verificable.

- **Riesgo: un item que solo no entra en el presupuesto de bytes.** Un comentario enorme podría
  producir una página vacía **con** cursor, que para el cliente es un bucle infinito.
  - **Mitigación:** se devuelve igual, con el campo de texto truncado y `"bodyTruncated": true`
    (RF-14). **Nunca una página vacía con cursor.**

- **Riesgo: `CREATE INDEX` bloquea escrituras.** Contra `objectives`, `requirements` y
  `objective_activity` en producción, un `CREATE INDEX` común toma un `SHARE` lock mientras
  construye.
  - **Mitigación:** evaluar `CREATE INDEX CONCURRENTLY` con la salvedad de que `sequelize-cli`
    envuelve cada migración en una transacción y `CONCURRENTLY` no puede correr adentro. Es la
    decisión de la story de índices, con el tamaño real de las tablas a la vista.

- **Riesgo: el contrato nace sin ningún consumidor en producción.** Al cerrar este REQ,
  `bus.query()` **sigue sin caller** (RF-37) y ninguna ruta `GET` migra. Nada ejercita los 23
  endpoints salvo los tests.
  - **Mitigación:** es una decisión de alcance, no un descuido — evita entregar el contrato y la
    migración de 28 rutas en el mismo salto. La contracara es que **la cobertura de tests es la
    única red**: los criterios de aceptación del REQ son el checklist, y `nats micro info` /
    `nats req` sobre un despliegue de `dev` son la verificación manual.

- **Riesgo: la documentación de arquitectura de `core` queda desalineada.**
  `docs/architectures/core/manifest.yaml` declara 5 módulos, todos carpetas bajo `src/commands/`, y
  `overview.md` dice "17 comandos" (hoy son 20) y describe tres capas sin mencionar el plano de
  consultas.
  - **Mitigación:** actualizar manifiesto y `overview.md` es parte del alcance de este REQ, no un
    "después": el módulo `queries` con sus 18 recursos es exactamente el tipo de cosa que el
    manifiesto existe para declarar. Va en la última story, cuando los 23 endpoints ya existen.

#### Dependencias

- **REQ-005 — dependencia bloqueante, YA SATISFECHA.** `Usuario.roles` e `identityType` existen
  (S-015), `core` espeja la identidad desde el evento (S-016), la compuerta por roles está
  implementada (S-017) y las personas pueden conectar al bus (S-018). Las cinco stories están en
  `Completed` y el request en `formalized`. **No hay bloqueo pendiente.**
- **S-013 — la infraestructura ya está entregada:** `jiku-queries` registrado con queue group
  propio, `readDb` con pool y `statement_timeout` propios, `QueryRegistry`, `QueryDispatcher` sin
  transacción, y los 6 stubs. Este REQ **construye encima**, no rehace.
- **S-014 — `bus.query()` con timeout de 10 s ya existe** en la api
  (`api/lib/utils/bus/index.ts:20,121`), deliberadamente sin caller. Este REQ pone a alguien a
  escuchar del otro lado y **no agrega el caller**.
- **Orden interno:** los índices y el envelope `errorDetails` + los 5 códigos son **precondiciones**
  de todo lo demás. `meta.describe` es **posterior** a las 18 fichas: se deriva de todas.
- **Sin dependencia con `deploy`:** las plantillas del callout publican sobre `jiku-queries.v1.>` y
  el comodín ya cubre los 23 métodos. **Ninguna plantilla cambia.**

#### Performance

- **Keyset y no offset.** Con `OFFSET`, la página N cuesta O(N) en PostgreSQL y una inserción
  concurrente hace que se salteen o repitan filas. La página siguiente se resuelve con
  `WHERE (sort…, id) > (k…)` sobre el índice — de ahí que **cada campo ordenable exija su índice
  compuesto terminado en `id`**.
- **`limit + 1` y no `COUNT`** para saber si hay página siguiente.
- **El presupuesto de bytes es la red de contención, no el mecanismo habitual.** El conjunto **base**
  deja afuera el texto sin cota, los campos calculados y los datos personales (RF-17), así que una
  página de 200 items tiene peso acotado por construcción. El recorte por bytes cubre el caso raro.
- **`count` cuesta un `COUNT` completo** con el mismo filtro y los mismos joins: por eso es opt-in,
  y por eso existe `count: "only"`, que **no ejecuta la consulta de filas**.
- **`requirements.totalMinutes` son dos subconsultas correlacionadas por fila.** Con `limit: 200`
  son 400. Es la razón por la que es incluible y no base, y por la que
  `worked_times(requirement_id)` y `worked_times(objective_id)` no son negociables.
- **`include` se resuelve con JOIN o con una consulta por lote de la página, nunca una por item**
  (RF-36). Las relaciones de colección vienen acotadas a los **10 más recientes** y marcan
  `"<relación>Truncated": true`.
- **Pool de lectura propio** (`POSTGRESQL_READ_POOL_MAX` = 10, separado del de escritura): una
  consulta pesada no le come conexiones a los comandos aunque compartan proceso.
- **Cachear es del llamador, no de este servicio.** Las consultas son idempotentes y sin efectos; el
  servicio no guarda estado entre requests.

#### Seguridad

- **La identidad sale del subject y solo de ahí** (RF-19). El auth-callout solo autoriza a publicar
  bajo el id propio, así que el segundo token es infalsificable. Un campo de identidad en el payload
  responde `invalid_fields` — no se ignora, se rechaza.
- **El cursor no autoriza nada** (RF-12). Es opaco, está atado a su `filter`+`sort` por el hash `h`,
  y **permisos, clase y filtros se reaplican en cada página**. Un cursor robado no da acceso a nada
  que el caller no pudiera pedir igual.
- **El recorte del modo externo se aplica antes del `filter` y no se puede desactivar por payload**
  (RF-22). Se inyecta en el SQL, no en el objeto de filtro del caller: no hay clave que lo pise.
- **Los `*_not_found` no distinguen "no existe" de "no lo podés ver"** (RF-31). Responder 403 le
  confirmaría a un usuario externo que el recurso existe.
- **El mensaje de error nunca incluye el subject completo** (RF-33), que lleva el user id. Los datos
  estructurados van en `errorDetails`.
- **`storageKey`, `storageBucket` y `storageRegion` nunca se devuelven**, ni como campo ni como
  filtro, y **esta API no mintea URLs** (RF-26, RF-27): la descarga sigue siendo el comando
  `files.{fileId}.request-download`, que es donde vive el efecto de firmar. Se excluyen siempre los
  vínculos con `deleted_at` y los archivos con `retention_status != 'active'`.
- **`ticketSlug` no se expone ni se acepta como filtro** (RF-26): la columna está marcada para
  eliminarse (FG-6) y el contrato nace sin ella.
- **`users.email` es el único dato personal del contrato**: se puede filtrar sin incluirlo, y solo
  viene si se lo pide explícitamente en `fields` o `include`.
- **`settings` sirve una lista blanca de claves**, no `system_settings` entera (RF-28): una clave no
  declarada **no existe** para esta API aunque exista en la tabla.
- **El modo interno no recorta filas, y es una decisión explícita de la v1** (RF-23): la autorización
  fina por rol (`admin` vs `user`) sigue siendo de la api sobre HTTP, y quien tiene credenciales de
  bus con rol `user` es equipo interno con acceso a todo.

### Flujos Afectados

#### Flujos Existentes Modificados

- **`autenticacion-y-entrada`** (ref: `docs/flows/autenticacion-y-entrada.md`)
  - Nuevo paso al final: **un segundo lugar donde una identidad sin fila en `users` bloquea el
    acceso.** Hoy el síntoma es un 401 `user_not_found` en HTTP; desde este REQ, un `unknown_caller`
    en toda consulta por el bus. Es la misma limitación trasladada a otra superficie.

- **`sincronizacion-de-identidades`** (ref: `docs/flows/sincronizacion-de-identidades.md`)
  - Consecuencia nueva: **la disponibilidad de la lectura por el bus queda acoplada a este flujo.**
    Como la entrega del evento no es durable (NATS sin JetStream), un evento perdido deja al caller
    sin fila y sus consultas devuelven `unknown_caller` hasta la próxima autenticación. Incluye al
    service user de la api, sin excepción por configuración.

- **`lectura-de-archivos`** (ref: `docs/flows/lectura-de-archivos.md`)
  - Nueva frontera explícita: **la descarga y el preview NO son consultas.** `files.get` y
    `attachments.list` devuelven metadatos y **no mintean ninguna URL**; obtener los bytes sigue
    siendo el comando `files.{fileId}.request-download`. El flujo gana la nota de que existe una
    superficie de lectura de metadatos por el bus que **no** lo reemplaza.

- **`vinculacion-de-archivos`** (ref: `docs/flows/vinculacion-de-archivos.md`)
  - Nueva superficie de lectura sobre el modelo que REQ-001 separó: `attachments.list` expone el
    vínculo con los datos del archivo aplanados, **traduciendo `entityType` en las dos direcciones**
    (`task` ↔ `objective`, `task_comment` ↔ `objective_comment`) y excluyendo siempre los vínculos
    con `deleted_at` y los archivos con `retention_status != 'active'`.
  - `docs/flows/adjuntos.md` está **Deprecated** desde REQ-001: se cita por trazabilidad y **no se
    modifica**.

#### Nuevos Flujos

- **`consulta-por-el-bus`**: el recorrido completo de una consulta, de la publicación al envelope.
  - **Trigger:** un caller publica en `{instance}.{user-id}.jiku-queries.v1.{recurso}.{operación}`
  - **Servicios:** caller (api, persona o conector) → NATS (`jiku-queries`, queue group propio) →
    `core` (`bus/service.ts` → `queries/dispatcher.ts` → ficha de recurso → motor) → PostgreSQL
    (`readDb`, rol de solo lectura)
  - **Cubre:** identidad desde el subject, resolución de la clase del caller, validación contra las
    listas blancas, recorte del modo externo antes del filtro, armado del SQL, paginación keyset con
    presupuesto de bytes, y las tres garantías duras — toda request obtiene respuesta, un `list`
    nunca devuelve `*_not_found`, y la ausencia de cursor es la única señal de fin.

### Referencias Externas Consultadas

No existe `docs/references/`. Los tres documentos de análisis que el request declara como insumo
viven sin trackear en la raíz del monorepo y S-013 los declara "no debe borrarse":

| Referencia | Relevancia | Restricciones Clave |
|------------|-----------|---------------------|
| `bus-api-consultas-ref.md` | **La referencia de implementación.** Los 23 endpoints, la gramática del payload y las cinco listas blancas por recurso. Es de donde salen literalmente las fichas de los 18 recursos | Listas **cerradas**: lo no declarado responde `invalid_fields`. `limit` default 50 / máx 200. Colecciones de `include` acotadas a 10 + `<relación>Truncated`. `entityType` obligatorio en `comments`/`activity`/`subscriptions`. `filter.projectId` obligatorio en `requirements.tags` |
| `bus-api-consultas.md` | El diseño completo y el porqué de cada decisión (§1–§16). Fija el formato del cursor (§7.1), el presupuesto de bytes (§7.3), el catálogo de errores y el mapa a HTTP (§8), los índices que la v1 necesita (§12) y las 25 reglas de implementación (§13) | Cursor `{v,k,h}` en base64url; `h` = hash de `filter`+`sort` normalizados. Presupuesto = `floor(max_payload * 0.5)`. `statement_timeout` (8000) **estrictamente menor** que el timeout de la request (10000). `sendCommand` y su mapa a HTTP **no se tocan** |
| `bus-micro-dos-servicios.md` | La infraestructura del servicio, ya entregada por REQ-004/S-013. §10.3 es el porqué de que `readDb` no registre los modelos | Dos servicios micro sobre **una** conexión NATS; queue group, pool y timeout separados; la conexión de lectura **no registra modelos de `@jiku/models`** |

> **Estos tres documentos son insumo, no contrato.** El contrato que queda tras este REQ es
> `docs/apis/core-queries.yaml`. Al cerrar la última story, los tres pueden archivarse.

## Story Split Propuesto

> **Formalizado el 2026-08-24 en nueve stories: S-020 a S-028.** El split se aceptó tal cual, sin
> agregar, quitar ni reordenar.
>
> | # de este split | Story | Título |
> |---|---|---|
> | 1 | **S-020** | El envelope gana `errorDetails` y el catálogo los cinco códigos de consulta |
> | 2 | **S-021** | Los índices compuestos que el keyset necesita |
> | 3 | **S-022** | El motor de consulta y el primer recurso: `tasks` |
> | 4 | **S-023** | La clase del caller y el recorte del modo externo |
> | 5 | **S-024** | Núcleo del dominio: `clients`, `projects` y `requirements` |
> | 6 | **S-025** | Comentarios, historial y suscripciones |
> | 7 | **S-026** | Personas, usuarios, tiempos y permisos |
> | 8 | **S-027** | Archivos en lectura: `attachments.list` y `files.get` |
> | 9 | **S-028** | Forma propia, `meta.describe` y el cierre del contrato |
>
> **Dos precondiciones de despliegue quedaron registradas en las stories y no estaban en este
> split:**
>
> 1. **S-021 se aplica en el entorno ANTES de desplegar el `core` de S-022.** Un `core` con contrato
>    contra una base sin índices es funcionalmente correcto y operativamente inviable.
> 2. **S-022 y S-023 se despliegan juntas a cualquier entorno con callers `external-user`.**
>    `ROLE_METHODS` ya autoriza `queries: ALL` a `external-user`, así que un `tasks.list` sin el
>    recorte del modo externo le devolvería a un externo tareas de proyectos que no ve. La separación
>    entre las dos es de **desarrollo y verificación**, no de despliegue independiente.
>
> **Documentación técnica actualizada al formalizar:** `docs/apis/core.yaml` (el envelope `Reply` gana
> `errorDetails`, `ErrorCode` gana los cinco códigos, y la nota de `info.description` deja de decir
> que el contrato de consultas no existe), `docs/db-schemas/jiku.md` (los índices del keyset en el
> DBML más la sección que fija la regla "ordenable ⇒ índice terminado en `id`"), el flujo nuevo
> `docs/flows/consulta-por-el-bus.md`, y los cuatro flujos existentes que el diseño declara
> modificados. **`docs/apis/core-queries.yaml` lo crea S-028**, cuando los 23 endpoints existen.

| # | Título | Descripción | Servicios | Dependencias |
|---|--------|-------------|-----------|--------------|
| 1 | El envelope gana `errorDetails` y el catálogo los cinco códigos de consulta | `errorDetails` opcional en `Reply`, `failure()` con tercer parámetro, y `unknown_caller`, `query_timeout`, `invalid_cursor`, `comment_not_found`, `task_not_found` en `@jiku/nats-protocol` y en el `enum` de `docs/apis/core.yaml`. Aditivo: los 20 comandos no cambian de comportamiento | `@jiku/nats-protocol`, `docs/apis/core.yaml` | - |
| 2 | Los índices compuestos que el keyset necesita | Una migración con los ~17 índices terminados en `id` de §12, más la verificación de los tres que ya existen. Sin ellos, ningún campo puede declararse ordenable | `api` (solo `db-upgrade/migrations/`), `docs/db-schemas/jiku.md` | - |
| 3 | El motor de consulta y el primer recurso: `tasks` | La gramática completa (`filter` con seis operadores y `or` de un nivel, `sort` con desempate por `id`, `page` keyset con cursor opaco y presupuesto de bytes, `fields`/`include`/`count`), la ficha de recurso como **dato**, `invalid_fields`/`invalid_cursor`/`query_timeout`, y `tasks.list`/`tasks.get` implementados end-to-end con la traducción `tasks`→`objectives` y `priority`/`priorityValue` | `core` | 1, 2 |
| 4 | La clase del caller y el recorte del modo externo | Un solo lookup a `users` por request que alimenta la compuerta de REQ-005 **y** la clase (conector/interno/externo, gana el más restrictivo); `unknown_caller` sin excepción por identidad; el recorte externo inyectado en el SQL **antes** del filtro y no desactivable por payload, aplicado a `tasks` | `core` | 3 |
| 5 | Núcleo del dominio: `clients`, `projects`, `requirements` | Seis endpoints con sus cuatro listas blancas, sus recortes externos, la traducción `properties`↔`key_value_pairs`, el filtro `tag` por par exacto, `q` que busca por `id` cuando es solo dígitos, y `totalMinutes` como incluible | `core` | 4 |
| 6 | Comentarios, historial y suscripciones | `comments.list/get`, `activity.list`, `subscriptions.list`: `entityType` obligatorio, las dos tablas (`objective_activity`/`requirement_activity`), `body`←`new_value`, `authorId`←`changed_by`, `attachments` embebidos con sus exclusiones, y `bodyTruncated` | `core` | 4 |
| 7 | Personas, usuarios, tiempos y permisos | `people.list`, `users.list`, `worked-times.list`, `unworked-times.list`, `week-assigned-times.list`, `project-permissions.list`: los tres recursos de tiempo **sin acceso externo**, `email` como incluible, y `user: null` para las personas sin usuario | `core` | 4 |
| 8 | Archivos en lectura: `attachments.list` y `files.get` | La traducción de `entityType` en las **dos** direcciones, la exclusión permanente de `storageKey`/`storageBucket`/`storageRegion`, el filtro por `retention_status = 'active'` y `deleted_at IS NULL`, `checksum` incluible, y **cero URLs minteadas** | `core` | 4 |
| 9 | Forma propia, `meta.describe` y el cierre del contrato | `requirements.tags`, `settings.list` por lista blanca de claves, y `meta.describe` **derivado** de las mismas fichas que validan. Se elimina `core/src/queries/pending.ts`, `nats micro info jiku-queries` lista **23**, y se actualizan `manifest.yaml` + `overview.md` de `core` y `docs/apis/core-queries.yaml` | `core`, `docs/architectures/core`, `docs/apis` | 5, 6, 7, 8 |

**Orden de implementación sugerido:**

1. **Story 1** — El envelope y los códigos. Es la única pieza en un paquete compartido, así que va
   primero y sola: el resto del REQ los usa desde la primera línea, y aislarla deja el cambio de
   `@jiku/nats-protocol` verificable por sí mismo contra los 20 comandos existentes.
2. **Story 2** — Los índices. **Puede ir en paralelo con la 1**: no comparte un solo archivo con
   ninguna otra story y es la única que toca `api/`. Va antes de la 3 porque RF-10 no deja declarar
   un campo ordenable sin su índice.
3. **Story 3** — El motor. Es **la pieza de mayor riesgo de diseño de todo el requerimiento** y por
   eso lleva un solo recurso encima: `tasks` concentra las tres trampas del contrato (la traducción
   de tabla, el enum ↔ entero con `priorityValue`, y el `VARCHAR` que no puede ser ordenable), así
   que si el motor está bien, está probado contra el caso difícil.
4. **Story 4** — La clase del caller. Después del motor y antes de los recursos: cada ficha declara
   su recorte externo, y el mecanismo tiene que existir antes de que haya 18 fichas que lo declaren.
   Separarla del motor es lo que permite que, si un recorte falla, se sepa si el bug es de la
   compuerta o de la consulta.
5. **Stories 5, 6, 7 y 8** — Los recursos. **Las cuatro son independientes entre sí y pueden ir en
   paralelo**: cada una agrega archivos nuevos bajo `src/queries/<recurso>/` y una línea en el
   registry. El agrupamiento no es por cantidad sino por **la dificultad que comparten**: la 6 es
   la familia de las dos tablas con ids que se pisan, la 7 la de los recursos sin acceso externo, y
   la 8 la de las exclusiones permanentes de storage.
6. **Story 9** — El cierre. Va última **por construcción**: `meta.describe` se deriva de todas las
   fichas y no puede escribirse antes de que existan, y `pending.ts` no puede borrarse mientras
   quede un endpoint sin contrato. Es también donde el conteo de `nats micro info` pasa de 6 a 23 y
   donde la documentación de arquitectura vuelve a describir el servicio que existe.
